package com.shinobicontrols.charts;

import android.graphics.drawable.GradientDrawable;

class ba extends GradientDrawable {
    ba(int i, int i2, float f) {
        setShape(0);
        setColor(i);
        setStroke(at.a(f, 1.0f), i2);
    }
}
